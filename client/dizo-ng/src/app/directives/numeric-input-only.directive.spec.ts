import { NumericInputOnlyDirective } from './numeric-input-only.directive';

describe('NumericInputOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericInputOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
