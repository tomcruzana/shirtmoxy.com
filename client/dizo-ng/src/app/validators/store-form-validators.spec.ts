import { StoreFormValidators } from './store-form-validators';

describe('StoreFormValidators', () => {
  it('should create an instance', () => {
    expect(new StoreFormValidators()).toBeTruthy();
  });
});
